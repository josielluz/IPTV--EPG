name: 📺 Gerar EPG IPTV

on:

  workflow_dispatch:

  schedule:
    # Executa diariamente às 03:00 UTC
    # = 00:00 no horário de Fortaleza
    - cron: "0 3 * * *"

  push:
    paths:
      - "channels.json"
      - "scripts/epg_generator.py"
      - "requirements.txt"
      - ".github/workflows/generate-epg.yml"

permissions:
  contents: write

jobs:

  generate:

    runs-on: ubuntu-latest

    steps:

      # ------------------------------------------------------
      # Checkout
      # ------------------------------------------------------

      - name: 📥 Checkout
        uses: actions/checkout@v4

      # ------------------------------------------------------
      # Python
      # ------------------------------------------------------

      - name: 🐍 Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      # ------------------------------------------------------
      # Dependências
      # ------------------------------------------------------

      - name: 📦 Instalar dependências
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      # ------------------------------------------------------
      # Gerar EPG
      # ------------------------------------------------------

      - name: 📺 Gerar EPG
        run: |
          python scripts/epg_generator.py

      # ------------------------------------------------------
      # Verificar arquivos
      # ------------------------------------------------------

      - name: 🔎 Verificar saída
        run: |
          echo "Arquivos gerados:"
          ls -lh output/

          echo ""
          echo "Tamanho do XML:"
          wc -c output/epg.xml

          echo ""
          echo "Canais XMLTV:"
          grep -c "<channel " output/epg.xml || true

          echo ""
          echo "Programas XMLTV:"
          grep -c "<programme " output/epg.xml || true

      # ------------------------------------------------------
      # Commit
      # ------------------------------------------------------

      - name: 💾 Commit e Push
        run: |

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add output/

          if git diff --cached --quiet; then

            echo "Nenhuma alteração nos arquivos EPG."

          else

            git commit -m "📺 Atualizar EPG IPTV"

            git push

          fi
